const {Given, When, Then} = require('@cucumber/cucumber');

Given("User at Overview Screen", () => {
    // TODO: Navigate app to overview screen
    return "pending";
});

When("User Clicks The First FlashCard", () => {
    // TODO: Get first flashcard in screen and click it
    return "pending";
});

When("User Clicks The Add FlashCard Button", () => {
    // TODO: Get the add flashcard button and click it
    return "pending";
});

When("User Fills FlashCard Form", () => {
    // TODO: Fill flashcard form in the add flashcard screen
    return "pending";
});

Given("User Clicks The Submit Button", () => {
    // TODO: Click the submit button in the add flashcard screen
    return "pending";
});

Given("Verify New FlashCard Appears", () => {
    // TODO: Verify that the new flashcard added appears in the overview screen
    return "pending";
});

Then("The First FlashCard Flips", () => {
    // TODO: Verify the first flashcard is flipped
    return "pending";
});

Then("Navigate to Add FlashCard Screen", () => {
    // TODO: Navigate to add flashcard screen
    return "pending";
});

Then("Navigate to Overview Screen", () => {
    // TODO: Navigate to overview screen
    return "pending";
});